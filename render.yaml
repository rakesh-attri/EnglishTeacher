type: web
services:
  - type: web
    name: voxflow-bilingual-voice-live
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
